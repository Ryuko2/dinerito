rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Reglas permisivas para desarrollo. Considera restringirlas en producción.
      allow read, write: if true;
    }
  }
}
